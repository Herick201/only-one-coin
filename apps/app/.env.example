# Provedor de backend (Postgres gerenciado + auth + storage) ainda a definir.
# Nenhuma var de provedor entra aqui até a decisão. NEXT_PUBLIC_* vai para o
# bundle do cliente: só chaves públicas aqui.

# Landing pública (Astro). Opcional; fallback para a raiz do próprio app.
# NEXT_PUBLIC_LANDING_URL=https://onlyonecoin.edu.pe
