---
import Base from "../layouts/Base.astro";
import InfoPage from "../components/InfoPage.astro";
import { content, defaultLang } from "../i18n/ui";

const title = `${content[defaultLang].nav.blog} — Only One Coin`;
---

<Base title={title}>
  <InfoPage kind="blog" />
</Base>
